% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constants.R
\name{fred_base_url}
\alias{fred_base_url}
\title{FRED / ALFRED API base URL}
\usage{
fred_base_url(url = env_or(var, default))
}
\arguments{
\item{url}{(scalar<character>) an explicit base URL override. Defaults to the
\code{FRED_BASE_URL} environment variable, or the public host when unset.}
}
\value{
(scalar<character>) the base URL.
}
\description{
The single host under which every FRED endpoint is addressed (e.g.
\verb{/fred/series/observations}, \verb{/fred/series}). FRED (latest values) and ALFRED
(as-known-then vintages) are the \strong{same host}, split only by the
\code{realtime_start} / \code{realtime_end} and \code{output_type} query parameters — there is
no separate ALFRED host. Overridable with the \code{FRED_BASE_URL} environment
variable.
}
