% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{fred_conditions}
\alias{fred_conditions}
\title{Typed fred conditions}
\description{
\code{fred} raises \strong{classed conditions} so a caller branches on error \emph{type} and
reads structured \emph{fields} instead of matching the message text.
}
\details{
\subsection{Class taxonomy}{
\itemize{
\item \strong{Transport failures} nest specific -> general as
\verb{fred_api_error_<status>} -> \code{fred_api_error} ->
\verb{connectcore_api_error_<status>} -> \code{connectcore_api_error} ->
\code{connectcore_error}, carrying the fields \code{status} (the HTTP status), \code{error_code}
(FRED's own error code from the response body), \code{url}, and \code{body_snippet}.
Raised for any non-2xx HTTP status, including the missing/invalid-key case
(FRED answers it with a plain HTTP 400 whose JSON body names the key problem;
unlike some APIs there is no HTML redirect to disentangle).
\item \strong{Validation failures} nest \code{fred_validation_error} -> \code{fred_error} (the
domain root). Raised for a malformed argument (an empty \code{series_id}, a
\code{realtime_end} before \code{realtime_start}, an unknown \code{search_type}) before any
request is made.
}

The \code{url} is stored with query-string credentials redacted (via
\code{\link[connectcore:scrub_url]{connectcore::scrub_url()}}, whose sensitive-parameter set matches both \code{key} and
\code{api_key}), so logging \code{e$url} never leaks the FRED key.
}
}
\seealso{
\link[connectcore:connectcore_conditions]{connectcore::connectcore_conditions}
}
