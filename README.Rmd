---
output: github_document
---

```{r, include=FALSE}
knitr::opts_chunk$set(
    warning = FALSE,
    message = FALSE,
    fig.path = "./man/figures/README-",
    fig.align = "center",
    fig.width = 12,
    fig.height = 10,
    dpi = 150,
    collapse = TRUE,
    comment = "#>"
)
```

# fred

R API wrapper to the St. Louis Fed's FRED and ALFRED economic-data API supporting both synchronous and asynchronous (promise based) operations. Provides an R6 client for series observations, as-known-then ALFRED vintages (paged for large matrices), series metadata, search, and releases, built on the shared connectcore transport base with a vintage-first, point-in-time-honest discipline.

## Features

| Feature | Description |
|---------|-------------|
| **testthat** | Unit testing framework with separate test files |
| **roxygen2** | Documentation generation from code comments |
| **pkgdown** | Automatic documentation website |
| **renv** | Reproducible dependency management |
| **GitHub Actions** | CI/CD for R-CMD-check and pkgdown deployment |
| **air** | Extremely fast R code formatter (Rust-based) |

## Project Structure

```
├── R/
│   └── add.R              # Pure R function example
├── tests/testthat/        # Unit tests
├── vignettes/             # Package vignettes
├── scripts/
│   └── BUILD.sh           # Build automation script
├── .github/workflows/     # CI/CD workflows
└── _pkgdown.yml           # Documentation site config
```

## Dependencies (renv)

This package ships with [renv](https://rstudio.github.io/renv/) — the
project library is isolated and pinned by `renv.lock`. First time after
cloning:

```r
renv::restore()
```

Adding a dependency:

```r
renv::install("somepkg")        # install into the project library
# add it to DESCRIPTION (Imports or Suggests), then:
renv::snapshot(dev = TRUE)      # record it in renv.lock (dev = TRUE includes Suggests)
```

CI restores the same lockfile (`r-lib/actions/setup-renv`), so checks run
against exactly the versions you develop with.

## Build Commands

```bash
# Full workflow: document → clean → build → check → install
./scripts/BUILD.sh all

# Quick development iteration (skip R CMD check)
./scripts/BUILD.sh quick

# Generate documentation only
./scripts/BUILD.sh document

# Format R code with air
./scripts/FORMAT.sh

# Check formatting (for CI)
./scripts/FORMAT.sh check

# Build pkgdown site
./scripts/BUILD.sh pkgdown

# Render README.Rmd to README.md
./scripts/BUILD.sh readme

# See all options
./scripts/BUILD.sh help
```

## Usage

```r
library(fred)

# Pure R function
add(1, 2)
#> [1] 3
```

## Installation

```r
# From GitHub (via renv)
renv::install("dereckscompany/fred")

# From CRAN (when available)
renv::install("fred")
```

### Requirements

- R >= 4.0
- [air](https://posit-dev.github.io/air/) for code formatting (optional)

## CI/CD

GitHub Actions workflows included:

- **R-CMD-check**: Tests on macOS, Windows, Ubuntu with multiple R versions
- **pkgdown**: Builds and deploys documentation to GitHub Pages

Both workflows use `concurrency` to cancel in-progress runs when new commits are pushed.

## License

MIT
