# Sort-order vocabulary

The accepted values of the `sort_order` parameter shared by the
observations, search, releases, and release-dates endpoints.

## Usage

``` r
SORT_ORDER
```

## Format

A named `list` of `scalar<character>` values: `asc`, `desc`.
